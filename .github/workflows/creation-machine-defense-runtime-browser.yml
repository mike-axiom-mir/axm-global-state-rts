name: Creation Machine defense static runtime

on:
  pull_request:
    branches:
      - main
    paths:
      - 'assets/creation-machine/**'
      - 'src/assets/creation-machine-defense-set.mjs'
      - 'src/assets/cached-static-glb-runtime.mjs'
      - 'src/world/starter-region.mjs'
      - 'src/sim/construction-economy.mjs'
      - 'game/app.mjs'
      - 'game/creation-machine-defense-adoption.mjs'
      - 'tests/creation-machine-defense-set-selftest.mjs'
      - 'tests/browser/creation-machine-defense-set.spec.js'
      - 'docs/CREATION_MACHINE_DEFENSE_STATIC_TRIAL_V0.md'
      - '.github/workflows/creation-machine-defense-runtime-browser.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checked-in-creation-machine-defense-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Check out exact RTS candidate and pinned Planet
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Use Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Prepare deterministic checked-in defense far derivatives
        run: |
          set -euo pipefail
          python3 assets/creation-machine/prepare_runtime_glb.py \
            --asset spotlight-tower \
            --variant far
          python3 assets/creation-machine/prepare_runtime_glb.py \
            --asset crane-section-watchtower \
            --variant far
          python3 assets/creation-machine/prepare_runtime_glb.py \
            --asset light-tower \
            --variant far

      - name: Verify source identity, stable ID and real state targets
        run: node tests/creation-machine-defense-set-selftest.mjs

      - name: Syntax-check focused runtime seam
        run: |
          node --check src/assets/creation-machine-defense-set.mjs
          node --check game/creation-machine-defense-adoption.mjs
          node --check tests/creation-machine-defense-set-selftest.mjs
          node --check tests/browser/creation-machine-defense-set.spec.js

      - name: Regress prior asset lanes and shell contracts
        run: |
          node tests/creation-machine-starter-set-selftest.mjs
          node tests/creation-machine-crew-set-selftest.mjs
          node tests/creation-machine-resource-set-selftest.mjs
          npm run test:shell

      - name: Install pinned Playwright browser test route
        run: npm install --no-save @playwright/test@1.55.0

      - name: Install Chromium runtime
        run: npx playwright install --with-deps chromium

      - name: Start actual Global State RTS shell with prepared defense derivatives
        run: |
          node scripts/serve.mjs > /tmp/axm-global-state-rts-creation-machine-defense.log 2>&1 &
          for i in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4174/game/ > /dev/null \
              && curl --fail --silent http://127.0.0.1:4174/assets/creation-machine/runtime-prepared/spotlight-tower-lod1.glb > /dev/null \
              && curl --fail --silent http://127.0.0.1:4174/assets/creation-machine/runtime-prepared/spotlight-tower-lod1.receipt.json > /dev/null \
              && curl --fail --silent http://127.0.0.1:4174/assets/creation-machine/runtime-prepared/crane-section-watchtower-lod1.glb > /dev/null \
              && curl --fail --silent http://127.0.0.1:4174/assets/creation-machine/runtime-prepared/crane-section-watchtower-lod1.receipt.json > /dev/null \
              && curl --fail --silent http://127.0.0.1:4174/assets/creation-machine/runtime-prepared/light-tower-lod1.glb > /dev/null \
              && curl --fail --silent http://127.0.0.1:4174/assets/creation-machine/runtime-prepared/light-tower-lod1.receipt.json > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/axm-global-state-rts-creation-machine-defense.log
          exit 1

      - name: Exercise explicit defense alternates in four-seat LOCAL RTS
        run: |
          npx playwright test tests/browser/creation-machine-defense-set.spec.js --workers=1 --reporter=line
          mkdir -p evidence/creation-machine-defense-runtime
          cp test-results/global-state-rts-creation-machine-spotlight-tower-4seat.png evidence/creation-machine-defense-runtime/
          cp test-results/global-state-rts-creation-machine-crane-section-watchtower-4seat.png evidence/creation-machine-defense-runtime/
          cp test-results/global-state-rts-creation-machine-light-tower-4seat.png evidence/creation-machine-defense-runtime/

      - name: Preserve defense runtime evidence without committing generated derivatives
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: global-state-rts-creation-machine-defense-runtime-evidence
          path: |
            assets/creation-machine/runtime-prepared/spotlight-tower-lod1.receipt.json
            assets/creation-machine/runtime-prepared/crane-section-watchtower-lod1.receipt.json
            assets/creation-machine/runtime-prepared/light-tower-lod1.receipt.json
            evidence/creation-machine-defense-runtime/*.png
            test-results/global-state-rts-*.png
            /tmp/axm-global-state-rts-creation-machine-defense.log
          if-no-files-found: warn
          retention-days: 14
